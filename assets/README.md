Letakkan QRIS statis Anda di path ini dengan nama:

- `assets/qris.png`

Bot akan membaca variabel `STATIC_QRIS` dari `.vars.json`.
Jika tidak diisi, default path yang dipakai adalah `assets/qris.png`.
